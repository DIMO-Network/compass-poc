apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "compass-poc.fullname" . }}-config
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "compass-poc.labels" . | nindent 4 }}
data:
{{- range $key, $val := .Values.env }}
  {{ $key }} : {{ $val | quote}}
{{- end}}
