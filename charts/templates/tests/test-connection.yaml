apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "compass-poc.fullname" . }}-test-connection"
  labels:
    {{- include "compass-poc.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "compass-poc.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
